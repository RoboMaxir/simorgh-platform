import{audit}from "../api/audit.js";const esc=x=>String(x??"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));async function run(){const el=document.getElementById("logs-table");try{const d=await audit.events(),events=d?.data?.events||d?.events||[];if(!events.length){el.innerHTML='<div class="empty-state">No audit events returned by the current API.</div>';return}el.innerHTML=`<div class="table-wrap"><table class="table"><thead><tr><th>Action</th><th>Resource</th><th>Resource ID</th><th>Metadata</th></tr></thead><tbody>${events.map(x=>`<tr><td>${esc(x.action)}</td><td>${esc(x.resource)}</td><td>${esc(x.resource_id)}</td><td>${esc(JSON.stringify(x.metadata||{}))}</td></tr>`).join("")}</tbody></table></div>`}catch(e){document.getElementById("logs-status").textContent="UNAVAILABLE";el.innerHTML=`<div class="state">${esc(e.message)}</div>`}}run();