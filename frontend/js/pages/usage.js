import{ai}from "../api/ai.js";const esc=x=>String(x??"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));const fmt=n=>Number(n||0).toLocaleString();async function run(){try{const d=await ai.usage();document.getElementById("usage-summary").innerHTML=`<div class="metric-grid"><article class="metric"><span class="label">Requests</span><strong>${fmt(d.total_requests)}</strong></article><article class="metric"><span class="label">Tokens</span><strong>${fmt(d.total_tokens)}</strong></article></div>`;const rows=d.entries||[];document.getElementById("usage-table").innerHTML=rows.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Timestamp</th><th>Provider</th><th>Model</th><th>Operation</th><th>Tokens</th><th>Status</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${esc(x.timestamp)}</td><td>${esc(x.provider)}</td><td>${esc(x.model)}</td><td>${esc(x.operation)}</td><td>${fmt(x.total_tokens)}</td><td>${esc(x.status)}</td></tr>`).join("")}</tbody></table></div>`:'<div class="empty-state">No usage entries returned.</div>'}catch(e){document.getElementById("page-status").textContent="UNAVAILABLE";document.getElementById("usage-summary").innerHTML=`<div class="state">${esc(e.message)}</div>`}}run();